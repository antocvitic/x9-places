delete all indexes and stored data in solr by deleting the contents of the folder <solr-path>/solr/data (perhaps)

copy schema.xml to <solr-path>/solr/conf/schema.xml

add and edit fields in schema.xml starting at row ~429